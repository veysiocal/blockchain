{
  "name": "hardhat-project",
  "scripts": {
    "deploy": "npx hardhat run --network localhost scripts/infinity-contract-deploy.ts",
    "deploy-rinkeby": "npx hardhat run --network rinkeby scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.2"
  }
}
